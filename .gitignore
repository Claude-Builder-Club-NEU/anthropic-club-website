# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Headshot derivatives are generated from board-src/ by the prebuild step.
# The masters are committed; these are build output.
public/board/

# Blog image derivatives are generated from blog-src/ by the prebuild step.
# The masters in blog-src/ are committed; these are build output.
public/blog-img/

# Environment — never commit secrets to this public repo
.env
.env.*
!.env.example
src/.env

# Editor and tooling config (personal, not project code)
.claude/
.codex/
.cursor/
.agents/
.gemini/
.kiro/
.opencode/
.pi/
.qoder/

# Design-tooling sidecar: DESIGN.md itself IS project data and is tracked
# deliberately. Only the local live-preview config is ignored.
.impeccable/live/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
