node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local

# Generated at build time from tokens.json and src/lib/event.js. Committed
# copies would be a second source of truth that can silently go stale.
src/styles/tokens.css
public/hacking-starts.ics
